GENERATE_SOURCEMAP=false
SKIP_PREFLIGHT_CHECK=true
# REACT_APP_AI_BACKEND_URL is intentionally NOT committed.
# Put it in .env.development.local (npm start) or .env.production.local (npm run build).
# See .env.example for details.